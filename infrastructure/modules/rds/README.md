> ⚠️ **STUB** — this module is a design reference, not functional Terraform. See `infrastructure/README.md`.

# Module: rds

PostgreSQL + pgaudit. The protected data resource for the dynamic-secrets demo (UC2/UC3).

**Status:** stays.
