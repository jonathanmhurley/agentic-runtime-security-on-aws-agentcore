# Module: vpc

**Slimmed.** Only needed for Vault EC2/Fargate reachability and RDS — not for an EKS cluster. Confirm the AgentCore Runtime → Vault network path (public endpoint vs VPC) during deploy design (DESIGN §8 item 4).

**Status:** stays (reduced scope).
