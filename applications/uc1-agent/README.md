# uc1-agent

Strands agent for Use Case 1, packaged for **AgentCore Runtime** (no Dockerfile->ECR->pod). Gets its workload identity (ARN) from AgentCore Identity; authenticates to Vault via the user-context JWT.
